# parsers/ (not implemented in v1)

Roadmapped for v1.5 per Project Plan V2 Section 11. Will hold structured
config-file input support (e.g., `docker-compose.yml`, AWS IoT policies, OTA
config files) so the reviewer can cite line-level evidence
(`firmware_update.yaml:18`) instead of only free-text prose.

v1 (`SKILL.md`) is free-text-only by design — see the locked v1 spec's
explicit non-goals. Do not add parsing logic here until v1.5 is scoped.
